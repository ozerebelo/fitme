{
  "name": "@fitme/core",
  "version": "0.1.0",
  "private": true,
  "description": "Domain layer for FitMe: energy balance, macronutrient targets, strength progression, program generation and the coaching engine. Consumed as TypeScript source by the apps (see transpilePackages), so it has no build step of its own.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.9.0",
    "vitest": "^3.2.0"
  }
}
